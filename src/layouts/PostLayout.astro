---
import ProgressBar from "../components/misc/ProgressBar";
import { bodyMargin } from "../styles/utility";
import BaseLayout from "./BaseLayout.astro";
import Title from "../components/misc/Title.astro";

const { frontmatter } = Astro.props;
---

<ProgressBar client:load />

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  image={frontmatter.image.url}
  url={`https://lunakit.netlify.app/posts/${frontmatter.title}`}
>
  <main class={`${bodyMargin}`}>
    <article>
      <Title frontmatter={frontmatter} />
      <div class="markdown">
        <slot />
      </div>
    </article>
  </main>
</BaseLayout>
